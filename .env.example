# Optional overrides for local monorepo development.
# Copy to .env.local — pnpm dev does not load these automatically;
# export them in your shell or use direnv.

OPENTIDE_DOCS_PATH=../opentide/docs
SPECIFICATIONS_PATH=../specifications
